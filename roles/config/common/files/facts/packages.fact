#!/bin/sh

SSH_PKG=''

if [ -f /etc/arch-release ]; then
	SSH_PKG=openssh
elif [ -f /etc/debian-release ] || [ -f /etc/fedora-release ]; then
	SSH_PKG='openssh-server'
fi

printf '{"ssh": "%s"}' "${SSH_PKG}"
